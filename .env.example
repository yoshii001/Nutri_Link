# Example environment variables for NutriLink
# Copy this file to `.env` and fill in real values for local development.
# Do NOT commit real secrets to source control.

# Firebase (public) - Expo expects EXPO_PUBLIC_* keys for runtime access
EXPO_PUBLIC_FIREBASE_API_KEY=your_api_key_here
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain
EXPO_PUBLIC_FIREBASE_DATABASE_URL=your_database_url
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
EXPO_PUBLIC_FIREBASE_APP_ID=your_app_id
EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# Optional: if you have non-public keys you don't want in the JS bundle,
# provide them as FIREBASE_* (without EXPO_PUBLIC_) and map them in app.config.ts
# FIREBASE_SERVICE_ACCOUNT_KEY=...